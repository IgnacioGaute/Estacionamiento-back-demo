// Generated browser artifacts from the SAME pricing sources used by the backend.
const fs = require('node:fs');
const path = require('node:path');
const ts = require('typescript');
const target = path.resolve(__dirname, '../../estacionamiento-front-demo/public');
for (const file of ['pricing', 'pricing.types', 'stay-pricing']) {
  const source = fs.readFileSync(path.resolve(__dirname, '../src/tickets/pricing', file + '.ts'), 'utf8');
  const result = ts.transpileModule(source, { compilerOptions: { target: ts.ScriptTarget.ES2020, module: ts.ModuleKind.ES2020, esModuleInterop: true } });
  const code = result.outputText.replaceAll("'@nestjs/common'", "'./offline-errors.js'").replaceAll("'dayjs/plugin/utc'", "'./offline-utc-module.js'").replaceAll("'dayjs/plugin/timezone'", "'./offline-timezone-module.js'").replaceAll("'dayjs'", "'./offline-dayjs-module.js'").replaceAll("'./pricing'", "'./offline-pricing.js'").replaceAll("'./pricing.types'", "'./offline-pricing.types.js'");
  fs.writeFileSync(path.join(target, 'offline-' + file + '.js'), '// GENERATED by scripts/build-offline-pricing.cjs; do not edit.\n' + code);
}
for (const [dependency, name] of [['dayjs', 'dayjs'], ['dayjs/plugin/utc', 'utc'], ['dayjs/plugin/timezone', 'timezone']]) fs.copyFileSync(require.resolve(dependency), path.join(target, 'offline-' + name + '-vendor.js'));
